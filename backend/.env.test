# Used only when running the test suite (npm test). A separate database keeps tests
# from touching real dev data — see database/README.md.
DATABASE_URL="mysql://root:gurupriya%23456@localhost:3306/electrostock_test"

PORT=5001
JWT_SECRET=test-secret-not-for-production
JWT_EXPIRES_IN=1h
CORS_ORIGIN=http://localhost:5173
