# Seeds

Sample/initial data for local development and testing of the ElectroStock
MySQL database.

Add `.sql` files here for raw data inserts, or reference/adjacent seed
scripts used by Prisma's seeding mechanism (`prisma db seed`), which is
configured and run from the `backend` folder.
